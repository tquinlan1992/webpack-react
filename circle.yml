machine:
  node:
      version: 8.9.0
  services:
      - docker

test:
  override:
      - npm install
      - npm run build-client
run:
    - docker build --rm=false -t $DOCKER_USER/webpack-react:$CIRCLE_BRANCH .
    - docker run --name app $DOCKER_USER/webpack-react:$CIRCLE_BRANCH
