machine:
  node:
      version: stable
  services:
      - docker

dependencies:
  override:
      - docker info
      - npm install

test:
  override:
      - npm run build-client
      - docker build --rm=false -t $DOCKER_USER/webpack-react:$CIRCLE_BRANCH .
